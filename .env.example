# Required Environment Variables
NEXT_PUBLIC_DEFAULT_API_ENDPOINT=https://your-validator-api.com/validate/
NEXT_PUBLIC_DEFAULT_REWORKD_BASE_URL=https://app.reworkd.ai/groups/your-group-id/root/

# Note: NEXT_PUBLIC_ prefix is required for client-side access in Next.js

# Authentication Groups (comma-separated list of allowed groups for settings access)
NEXT_PUBLIC_ALLOWED_GROUPS=group1,group2,group3
